spring:
  profiles: db
  datasource:
        url: jdbc:mariadb://fbbdb.cuenvx3cdzkh.ap-northeast-2.rds.amazonaws.com:3306/fbbdb
        username: dolphin
        password: Dolphin13#
        driver-class-name: org.mariadb.jdbc.Driver
  jpa:
      hibernate:
       ddl-auto: update
      show-sql: true # 실행 되는 SQL문을 보여 줄지 설정
      properties:  
        hibernate.format_sql: true # 형식화된 sql 문을 보여준다. Beautifier 기능이라고 생각하면 된다
        hibernate.enable_lazy_load_no_trans: true # Lazy Load 를 적용하기 위한 설정
      database: mysql